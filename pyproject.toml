[project]
name = "tumblr-backup-cookie-scrape"
version = "0.1.0"
description = "Back up a list of tumblr blogs (for use in automated backups)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "tumblr-backup[bs4,dash,jq,video]>=1.6.3",
]
